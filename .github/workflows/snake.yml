name: Generate Snake Animation

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  generate-snake:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate Snake Animation
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: clerin-codes
          outputs: |
            dist/github-snake.svg?color_snake=lime&color_dots=#1a1a2e,#1f6b2e,#2ea043,#3dba56,#56d364&color_empty=#1a1a2e
            dist/github-snake-dark.svg?palette=github-dark&color_snake=lime&color_dots=#0d1117,#1f6b2e,#2ea043,#3dba56,#56d364&color_empty=#0d1117

      - name: Push snake to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
          keep_history: false
          allow_empty_commit: false
          fqdn: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
